- name: Setup backend and deploy HCI
  hosts: hc_nodes
  remote_user: root
  gather_facts: yes

  vars:
     gluster_features_hci_cluster: "{{ groups['hc_nodes'] }}"

  roles:
     - gluster.infra
     - gluster.features
